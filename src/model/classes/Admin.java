package model.classes;

public class Admin extends User{
    public Admin(String name, String cellphone, String email, String password, int id) {
        super(name, cellphone, email, password, id);
    }
}
